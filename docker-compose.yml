services:
  neo4j:
    image: neo4j:5.19
    container_name: neo4j
    ports:
      - "7474:7474"   # HTTP Browser UI
      - "7687:7687"   # Bolt protocol
    environment:
      NEO4J_AUTH: neo4j/test1234
      NEO4J_dbms_connector_bolt_advertised__address: localhost:7687
      NEO4J_dbms_connector_http_advertised__address: localhost:7474
    volumes:
      - neo4j-data:/data
      - /$HOME/neo4j/logs:/logs
      - /$HOME/neo4j/config:/config
      - /$HOME/neo4j/data:/data
      - /$HOME/neo4j/plugins:/plugins
    networks:
      - default
    restart: always
  
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    working_dir: /app
    depends_on:
      - backend

  backend:
    build: ./backend_2
    ports:
      - "8000:8000"
    volumes:
      - ./backend_2:/code/backend_2
    environment:
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=test1234
      - FLASK_APP=myapi.app:create_app
    depends_on:
      neo4j:
        condition: service_started
  spark-master:
    image: bitnami/spark:latest
    container_name: spark-master
    environment:
      - SPARK_MODE=master
    ports:
      - "7077:7077"
      - "8080:8080"
    networks:
      - default

  spark-worker-1:
    image: bitnami/spark:latest
    container_name: spark-worker-1
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
    depends_on:
      - spark-master
    networks:
      - default

  spark-worker-2:
    image: bitnami/spark:latest
    container_name: spark-worker-2
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
    depends_on:
      - spark-master
    networks:
      - default
  
  spark-job:
    build: ./spark
    container_name: spark-job
    depends_on:
      - spark-master
      - neo4j
    volumes:
      - ./spark:/app
    working_dir: /app
    environment:
      - PYSPARK_PYTHON=python3
    command: >
      python /opt/spark/nyc_crime_batch_to_neo4j.py
    networks:
      - default

volumes:
  neo4j-data:

networks:
  default:
    name: bigdata-net
